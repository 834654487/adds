#ifndef REVERSE_H
#define REVERSE_H
#include<iostream>
#include<string>
#include<math.h>
using namespace std;
class Reverse{
    public:
    Reverse();
    int reverseDigit(int value);
    std::string reverseString(std::string letters);
    
};
#endif